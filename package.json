{
  "name": "my-vue-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "electron/main.js",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "electron:dev": "VITE_DEV_SERVER_URL=http://localhost:5173 electron .",
    "electron:build": "pnpm build && electron-builder --win --publish never",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "big.js": "^7.0.1",
    "vue": "^3.5.31",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/big.js": "^6.2.2",
    "@types/node": "^24.12.0",
    "@vitejs/plugin-vue": "^6.0.5",
    "@vue/tsconfig": "^0.9.1",
    "@tauri-apps/cli": "^1.5.10",
    "electron": "^32.2.0",
    "electron-builder": "^24.13.3",
    "typescript": "~5.9.3",
    "vite": "^8.0.3",
    "vue-tsc": "^3.2.6"
  },
  "build": {
    "appId": "com.lvcdy.jiujing.electron",
    "productName": "Jiujing Electron",
    "files": [
      "dist/**",
      "electron/**",
      "package.json"
    ],
    "directories": {
      "output": "release/electron"
    },
    "win": {
      "target": "zip"
    },
    "artifactName": "jiujing-electron-${version}-windows-${arch}.${ext}"
  }
}