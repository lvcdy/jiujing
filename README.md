# 酒精浓度计算器

基于国家标准数据表，根据酒精计读数和当前温度，自动计算校正至 20℃ 标准状态下的**体积分数**与**质量分数**的桌面/网页工具。

> 适用于酿造、检测、销售等场景中对酒精浓度进行温度校正与单位换算的日常需求。

---

## 功能特性

### 核心计算

- 📊 **正向计算** — 输入酒精计读数与当前温度，一键计算 20℃ 标准浓度（体积分数 + 质量分数 + 相对密度）
- 🔄 **反向计算** — 输入目标标准浓度与温度，推算对应的酒精计读数
- 📐 **密度 ↔ 浓度互查** — 体积分数与相对密度双向查询
- 📈 **浓度图表** — 可视化展示温度-浓度关系曲线（基于 Chart.js）
- 📏 **不确定度估算** — 基于误差传播理论，给出测量结果的扩展不确定度（k=2）
- 📝 **计算过程展示** — 可展开查看完整的中间计算步骤与公式

### 通用功能

- 🌡️ **温度单位切换** — 支持 ℃ / °F 自由切换，设置自动保存至 localStorage
- 🌐 **中英文界面** — 支持简体中文与英文切换（i18next 国际化）
- ⌨️ **键盘快捷键** — Enter 计算、Tab 切换、Esc 清空，操作高效流畅
- 🖥️ **桌面应用** — 基于 Tauri 2 构建，轻量高性能（Windows）
- 🎯 **高精度计算** — 全程使用 `big.js`，避免浮点误差

---

## 技术栈

| 类别 | 技术 |
|------|------|
| UI 框架 | Svelte 5 + TypeScript |
| 构建工具 | Vite |
| 桌面封装 | Tauri 2 |
| 高精度计算 | big.js |
| 图表可视化 | Chart.js 4（Canvas 直接渲染） |
| 国际化 | i18next（纯 JS，无框架绑定） |
| 样式 | 纯 CSS（玻璃拟态风格） |

---

## 项目结构

```
src/
├── main.ts                  # 应用入口
├── app.d.ts                 # 类型声明
├── assets/
│   ├── jiujing.json         # 酒精浓度数据表
│   └── wendu.json           # 温度校正数据表
├── components/
│   ├── Calculator.svelte    # 主计算器组件
│   └── Calculator.css       # 样式（玻璃拟态风格）
├── i18n/
│   ├── index.ts             # i18next 初始化
│   └── locales/
│       ├── zh-CN.ts         # 中文翻译
│       └── en-US.ts         # 英文翻译
└── utils/
    └── data.ts              # 数据加载与插值计算核心
src-tauri/                   # Tauri 桌面端配置与 Rust 代码
excel/                       # 原始 Excel 数据文件（仅供参考）
```

---

## 环境要求

- **Node.js** ≥ 18（推荐使用 LTS 版本）
- **pnpm**（推荐）或 npm / yarn
- 打包 Tauri 版本时还需要安装 [Rust 工具链](https://www.rust-lang.org/tools/install)

---

## 安装与启动

```bash
# 安装依赖
pnpm install

# 启动网页开发服务器（浏览器预览）
pnpm dev

# 启动 Tauri 桌面应用（开发模式）
pnpm tauri dev

# 构建网页产物
pnpm build

# 构建 Tauri 桌面安装包（Windows NSIS）
pnpm tauri build

# 预览构建产物
pnpm preview
```

---

## 使用说明

应用通过顶部标签页切换四种功能模式：

### 正向计算

1. 在**酒精计读数**输入框中输入酒精计示数（单位 %）
2. 在**当前温度**输入框中输入测量时的环境温度（单位 ℃/°F）
3. 点击「**开始计算**」按钮或按 `Enter` 键
4. 结果区域将显示校正至 20℃ 的标准浓度（体积分数、质量分数、相对密度）及不确定度
5. 可展开**计算过程**查看完整的中间步骤与公式

### 反向计算

1. 输入**目标标准浓度**（20℃ 体积分数 % vol）
2. 输入当前**温度**
3. 计算得到对应的酒精计读数
4. 同样可展开查看详细计算过程

### 密度互查

- 输入**体积分数** → 查询对应的**相对密度**（及质量分数）
- 输入**相对密度** → 查询对应的**体积分数**

### 浓度图表

- 输入酒精计读数，生成该浓度下温度与标准浓度的关系曲线图

---

## 数据来源

计算所用数据基于国家标准酒精浓度-温度校正表，已预处理为 JSON 格式存储在 `src/assets/` 目录下。原始 Excel 文件保留在 `excel/` 目录中供参考。

---

## 许可证

MIT

### 键盘快捷键

| 按键 | 说明 |
|------|------|
| `Enter` | 两个输入框均有值时直接计算；仅第一个输入框有值时跳转至下一个输入框 |
| `Tab` | 在输入框之间切换焦点 |
| `Esc` | 清空当前输入框内容 |
| `Ctrl + A` | 全选当前输入框内容 |

---

## 国际化

应用支持**简体中文**与**英文**两种语言，可在界面右上角通过下拉菜单切换，语言设置会保存到 `localStorage`。

语言文件位于 `src/i18n/locales/`：

```
src/i18n/locales/
├── zh-CN.ts   # 简体中文
└── en-US.ts   # 英文
```

---

## 项目结构

```
jiujing/
├── src/
│   ├── assets/              # 数据文件（jiujing.json、wendu.json）
│   ├── components/
│   │   ├── Calculator.svelte  # 主计算器组件（Svelte 5，含全部四个功能标签页）
│   │   └── Calculator.css     # 组件样式（玻璃拟态风格）
│   ├── i18n/
│   │   ├── index.ts         # i18next 初始化（纯 JS，无框架绑定）
│   │   └── locales/         # 语言包（zh-CN / en-US）
│   ├── utils/
│   │   └── data.ts            # 数据加载、缓存与插值计算
│   └── main.ts              # 应用入口（Svelte mount）
├── excel/                   # 原始 Excel 数据文件（jiujing.xlsx、wendu.xlsx）
├── src-tauri/               # Tauri 配置与 Rust 代码
├── public/                  # 静态资源
├── index.html
├── vite.config.ts
├── svelte.config.js
└── package.json
```

---

## 算法与计算逻辑

### 1. 数据读取与结构化

- 通过 `fetch` 读取 `assets` 中的 JSON 数据文件
- 列坐标（酒精计读数）与行坐标（温度，降序）从数据中提取
- 解析结果缓存于内存 `Map` 中，避免重复加载

### 2. 坐标定位（二分查找）

- 目标输入为：酒精计读数与温度
- 对行坐标与列坐标分别做二分查找，找出目标值两侧的索引区间
- 支持坐标升序或降序的表格
- 若目标值超出边界，退化为最近边界值

### 3. 双线性插值（正向计算）

- 取四个角点：$v_{11}$、$v_{12}$、$v_{21}$、$v_{22}$
- 若行或列出现同值，退化为一维线性插值
- 双线性插值公式：

$$
f(x, y)=(1-t_x)(1-t_y)v_{11}+t_x(1-t_y)v_{12}+(1-t_x)t_y v_{21}+t_x t_y v_{22}
$$

其中：
- $t_x=(x-x_1)/(x_2-x_1)$，$t_y=(y-y_1)/(y_2-y_1)$

### 4. 反向插值（反向计算）

- 已知目标标准浓度和温度，反推酒精计读数
- 仅使用整数酒精读数列（跳过半步长列，因 Excel 数据在不同范围格式不一致）
- 在目标温度行中线性插值查找对应的酒精读数
- 若温度行非整数，在相邻两温度行间做第二次插值

### 5. 一维线性插值（质量分数与密度互查）

- 读取第二张表（体积分数 ↔ 质量分数 ↔ 相对密度）
- 对体积分数进行一维线性插值：

$$
f(x)=y_1+(x-x_1)\cdot\frac{y_2-y_1}{x_2-x_1}
$$

### 6. 不确定度估算

- 基于误差传播理论，通过有限差分法计算偏导数
- 扩展不确定度公式（k=2）：

$$
U = k\sqrt{\left(\frac{\partial V}{\partial A}\cdot\Delta A\right)^2+\left(\frac{\partial V}{\partial T}\cdot\Delta T\right)^2}
$$

### 7. 精度处理

- 全部插值计算使用 `big.js`，避免浮点误差
- 最终结果保留两位小数输出

---

## big.js 说明

`big.js` 是一个高精度小数计算库，用来避免 JavaScript 原生 `number` 的浮点误差（例如 `0.1 + 0.2 !== 0.3`）。它适合金额、浓度、比例等需要稳定舍入与可控精度的场景。

### 核心特点

- 十进制高精度：内部以十进制表示，避免二进制浮点误差
- API 简洁：提供加减乘除、比较、取整、格式化等常用操作
- 可配置精度与舍入方式：通过 `Big.DP` 与 `Big.RM` 控制结果

### 基础用法

```ts
import Big from "big.js";

const a = new Big("0.1");
const b = new Big("0.2");

const c = a.plus(b);        // 0.3
const d = a.times("3");     // 0.3

console.log(c.toString());  // "0.3"
```

### 精度与舍入

```ts
import Big from "big.js";

Big.DP = 10; // 小数位精度 (Decimal Places)
Big.RM = 1;  // 舍入模式 (0: RoundDown, 1: RoundHalfUp, 2: RoundHalfEven, 3: RoundUp)

const x = new Big("1").div("3");
console.log(x.toString());  // "0.3333333333"
```

### 比较

```ts
import Big from "big.js";

const a = new Big("2.50");
const b = new Big("2.5");

console.log(a.eq(b)); // true
console.log(a.gt(b)); // false
console.log(a.lt(b)); // false
```

### 在本项目中的使用

- 输入值与表格数值会先转换为 `Big` 实例，避免中间计算出现浮点误差
- 线性插值与双线性插值过程用 `Big` 的加减乘除完成，保证结果稳定
- 输出阶段再统一做精度处理（保留两位小数），避免早期舍入带来累积误差

---

本项目代码 **© 2026 lvcdy**。  
**未经书面许可，严禁以任何形式使用、复制、修改、分发或用于商业用途。**  
本仓库仅用于个人展示/学习存档，**不授予任何授权**。
